# Test environment variables for Postgres and MinIO
# Copy to .env and customize if needed.

# Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=deltalakedb

# MinIO
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=deltalakedb-test

# Optional: MinIO Console
MINIO_CONSOLE_ENDPOINT=http://localhost:9001